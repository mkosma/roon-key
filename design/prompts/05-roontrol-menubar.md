Locking the icon. Save to memory: RoonTrol primary mark is [chosen variation], troll-eye accent is #C9A24B.

Now the menubar glyph. This is different from the icon - it's a single-color silhouette used in the actual macOS menubar at 18x18. No squircle, no color, no accent.

Format: 64x64 PNG, transparent background (will be scaled to 18x18 in code).

Subject: the troll-under-bridge silhouette from the locked icon, reduced to a single solid black shape on transparent background. Strip out all internal detail; what remains is the silhouette - the bridge arch with the troll's head poking up under it. The eyes can become two small negative-space dots if it helps readability; otherwise omit them entirely.

Constraints: must be recognizable at exactly 18 pixels. This is a silhouette test - if the shape doesn't read at 18px, it fails. No gradients, no gray, no anti-aliasing tricks beyond standard. Verify by showing at 18x18 next to a 256x256 render.
